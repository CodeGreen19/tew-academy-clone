import { Table } from "@tanstack/react-table";
interface SelectedRowProps<TData> {
  table: Table<TData>;
}
export default function DataTableSelectedRowActions<
  TData
>({}: SelectedRowProps<TData>) {
  //   const selectedRows = table.getSelectedRowModel().rows.length;
  return null;
}
